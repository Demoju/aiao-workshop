<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tableorder.customer.mapper.OrderMapper">
    
    <insert id="insertOrder" parameterType="com.tableorder.domain.Order" 
            useGeneratedKeys="true" keyProperty="orderId">
        INSERT INTO orders (order_number, table_id, session_id, total_amount, status, order_time, created_at)
        VALUES (#{orderNumber}, #{tableId}, #{sessionId}, #{totalAmount}, #{status}, #{orderTime}, CURRENT_TIMESTAMP)
    </insert>
    
    <select id="selectOrdersBySession" resultType="com.tableorder.domain.Order">
        SELECT order_id, order_number, table_id, session_id, total_amount, 
               status, order_time, created_at, updated_at
        FROM orders
        WHERE table_id = #{tableId}
          AND session_id = #{sessionId}
        ORDER BY order_time DESC
    </select>
    
    <select id="selectOrderById" resultType="com.tableorder.domain.Order">
        SELECT order_id, order_number, table_id, session_id, total_amount, 
               status, order_time, created_at, updated_at
        FROM orders
        WHERE order_id = #{orderId}
    </select>
    
    <delete id="deleteOrder">
        DELETE FROM orders
        WHERE order_id = #{orderId}
    </delete>
    
    <select id="countTodayOrders" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM orders
        WHERE DATE(order_time) = CURRENT_DATE
    </select>
    
</mapper>
