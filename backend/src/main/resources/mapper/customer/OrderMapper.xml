<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tableorder.customer.mapper.OrderMapper">
    
    <insert id="insertOrder" parameterType="com.tableorder.domain.Order" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (order_number, store_id, table_id, session_id, total_amount, status, created_at)
        VALUES (#{orderNumber}, #{storeId}, #{tableId}, #{sessionId}, #{totalAmount}, #{status}, CURRENT_TIMESTAMP)
    </insert>
    
    <select id="selectOrdersBySession" resultType="com.tableorder.domain.Order">
        SELECT id, order_number, table_id, session_id, total_amount, 
               status, created_at, updated_at
        FROM orders
        WHERE table_id = #{tableId}
          AND session_id = #{sessionId}
        ORDER BY created_at DESC
    </select>
    
    <select id="selectOrderById" resultType="com.tableorder.domain.Order">
        SELECT id, order_number, table_id, session_id, total_amount, 
               status, created_at, updated_at
        FROM orders
        WHERE id = #{orderId}
    </select>
    
    <delete id="deleteOrder">
        DELETE FROM orders
        WHERE id = #{orderId}
    </delete>
    
    <select id="countTodayOrders" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM orders
        WHERE DATE(created_at) = CURRENT_DATE
    </select>
    
</mapper>
