<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tableorder.customer.mapper.MenuMapper">
    
    <select id="selectMenus" resultType="com.tableorder.domain.Menu">
        SELECT id as menuId, name as menuName, price, description, image_url as imageUrl, 
               category_id as categoryId, store_id as storeId, is_available as isAvailable, 
               created_at as createdAt, updated_at as updatedAt
        FROM menu
        WHERE is_available = true
        <if test="categoryId != null">
            AND category_id = #{categoryId}
        </if>
        ORDER BY id
    </select>
    
    <select id="selectMenuById" resultType="com.tableorder.domain.Menu">
        SELECT id as menuId, name as menuName, price, description, image_url as imageUrl, 
               category_id as categoryId, store_id as storeId, is_available as isAvailable, 
               created_at as createdAt, updated_at as updatedAt
        FROM menu
        WHERE id = #{menuId}
    </select>
    
</mapper>
