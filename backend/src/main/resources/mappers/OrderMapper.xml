<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tableorder.mapper.OrderMapper">
    
    <select id="findBySessionIdIn" resultType="com.tableorder.domain.Order">
        SELECT id, store_id, table_id, session_id, order_number, status, total_amount, created_at, updated_at
        FROM orders
        WHERE session_id IN
        <foreach collection="sessionIds" item="sessionId" open="(" separator="," close=")">
            #{sessionId}
        </foreach>
        ORDER BY created_at DESC
    </select>
    
    <select id="findById" resultType="com.tableorder.domain.Order">
        SELECT id, store_id, table_id, session_id, order_number, status, total_amount, created_at, updated_at
        FROM orders
        WHERE id = #{orderId}
    </select>
    
    <update id="updateStatus">
        UPDATE orders
        SET status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{orderId}
    </update>
    
    <delete id="deleteById">
        DELETE FROM orders WHERE id = #{orderId}
    </delete>
    
</mapper>
